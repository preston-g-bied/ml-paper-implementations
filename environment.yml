name: ml-papers
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - pandas
  - matplotlib
  - seaborn
  - scikit-learn
  - jupyter
  - notebook
  - ipywidgets
  - tqdm
  - pillow
  # Deep learning packages (uncomment as needed)
  # - pytorch
  # - tensorflow
  # - keras
  # - jax
  # - flax
  # Optional packages
  - pytest
  - black
  - flake8
  - pip
  - pip:
    - wandb  # For experiment tracking
    # Add other pip packages here